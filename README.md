In your project directory, you run:

# npm install

# npm start:

Location: http://localhost:3000

Json Server needs to run in order to add / delete posts from data/db.json

To run json-server - Open a new terminal window and run command:
npx json-server --watch data/db.json --port 8000
